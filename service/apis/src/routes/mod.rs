//pub mod patient;
//pub mod staff;
//pub mod appointment;

pub fn register_routes(_cfg: &mut actix_web::web::ServiceConfig) {
    //patient::init(cfg);
    //staff::init(cfg);
    //appointment::init(cfg);
}