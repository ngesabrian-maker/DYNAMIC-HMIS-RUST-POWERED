version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hmis
      POSTGRES_PASSWORD: hmis123
      POSTGRES_DB: hmis
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
